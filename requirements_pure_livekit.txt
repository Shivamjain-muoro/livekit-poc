# Pure LiveKit Interview Agent Requirements
# Following Friday/Jarvis pattern

# Core LiveKit Agent framework
livekit-agents>=0.8.0
livekit-api>=0.5.0
livekit-rtc>=0.8.0

# LiveKit plugins for voice processing
livekit-plugins-google>=0.5.0

# Environment and utilities
python-dotenv>=1.0.0

# AI for question generation
google-generativeai>=0.3.0

# Optional: Enhanced audio processing
# livekit-plugins-deepgram  # For alternative STT
# livekit-plugins-elevenlabs  # For alternative TTS
